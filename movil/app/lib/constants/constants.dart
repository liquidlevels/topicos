import 'package:flutter/material.dart';

//Text

const kAppTitle = 'uwu';
const kHeaderTitle = 'awita de uwu';

//Colors

const kPrimaryColor = Color(0xff0a0908);
const kSecondaryColor = Colors.deepPurple;
const kTextColor = Color(0xffffffff);

//Styles

const kTextStyleHeaderTitle =
    TextStyle(color: kTextColor, fontSize: 40, fontWeight: FontWeight.bold);

const kTextStyleFruitsText = TextStyle(color: kTextColor, fontSize: 16);
